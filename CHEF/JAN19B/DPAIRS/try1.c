#include <stdio.h>
#include <stdbool.h>
#define unsigned long long ulli

ulli maxl= 200000;

int main()
{
        ulli n, m;
        ulli a[maxl][2], b[maxl][2];
        ulli i;

        scanf("%llu%llu", &n, &m);
        for(i=0; i<n; i++)
        {
                scanf("%llu",&a[i][0])
                a[i][1]=i;
        }

        sort("")
        
        return 0;
}
